#%%
window = np.random.normal(0, 0.37, size=40)
plt.plot(window)
qstep = 8
qfunct = np.linspace(0, max(window), qsteps)
qidx = np.digitize(window, qfunct)
qwindow [qevents[idx-1] for idx in qidx]
plt.plot(qwindow)
#%%

